[metadata]
name = conidie
version = 0.0.1
classifiers =
    Framework :: napari

[options]
packages = find:
include_package_data = True

# add your package requirements here
install_requires =
    napari
    scikit-image

[options.entry_points]
napari.manifest =
    conidie = conidie:napari.yaml